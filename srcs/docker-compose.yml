services:
  nginx:
    build: ./requirements/nginx
    ports:
      - "443:443"
    networks:
      - wordpress-net
    restart: on-failure

  wordpress:
    build: ./requirements/wordpress
    volumes:
      - wordpress:/volumes/wordpress:/var/www/html
    networks:
      - wordpress-net
    depends_on:
      - mariadb
    restart: on-failure
    expose: 
      - "9000"

  mariadb:
    build: ./requirements/mariadb
    env_file: .env
    volumes:
      - mariadb:/volumes/db:/var/lib/mysql
    networks:
      - wordpress-net
    restart: unless-stopped
    expose:
      - "3306"

volumes:
  wordpress:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/home/login/data/wordpress'
  mariadb:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/home/login/data/mariadb'
  
networks:
  wordpress-net:
    driver: bridge
